# true = in-browser mock data (no Docker). false = Python microservices via Vite proxy.
VITE_USE_MOCKS=true

# Optional: immediate (default) = ACTIVE + equipment rented; pending_payment = PENDING until finalize-booking.
# VITE_CHECKOUT_MODE=pending_payment

# Legacy Flask reference API (mock + old backend only).
# VITE_API_BASE_URL=http://localhost:4000
